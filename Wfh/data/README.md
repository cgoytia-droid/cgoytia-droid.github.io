# data/ — poné acá tus datos

Colocá el shapefile/GeoPackage de unidades espaciales y, si corresponde, la
tabla de atributos (CSV/RDS). Configurá las rutas y nombres de columnas en
`R/_config.R`. El contenido de esta carpeta no se versiona (ver .gitignore).
